# Legacy / Experimental Area

This folder is not the active production Express runtime.

Active production code lives at the repository root:

- `server.js`
- `routes/`
- `services/`
- `middleware/`

Do not implement production fixes in this folder unless the user explicitly asks to work on legacy/kernel experiments.

If you are fixing production behavior, go back to the root folders.

